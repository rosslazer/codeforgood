<table>
  <tr>
    <th>Name</th>
    <th>Location</th>
    <th>Working?</th>
    <th>Return Date</th>
  </tr>

<% @tools.each do |tool| %>
  <tr>
    <td><%= tool.name %></td>
    <td><%= tool.location_id %>
    <td><%= tool.working %></td>
    <td><%= tool.return_date %>
  </tr>
<% end %>
</table>

<br />
